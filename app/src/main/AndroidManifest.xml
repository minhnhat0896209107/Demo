<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.loginapp">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LoginApp">
        <activity android:name=".activities.target.TargetActivity"/>
        <activity android:name=".activities.chat.ChatActivity" />
        <activity android:name=".activities.proflie.ProfileActivity" />
        <activity android:name=".activities.achievement.AchievementActivity" />
        <activity android:name=".activities.home.HomeActivity" />
        <activity android:name=".activities.forgot.ForgotPasswordActivity" />
        <activity android:name=".activities.signup.SignUpActivity" />
        <activity android:name=".activities.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>